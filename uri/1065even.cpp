#include <bits/stdc++.h>

using namespace std;

int main() 
{	
	int n,count=0;
	for (int i = 0; i < 5; ++i)
	{
		scanf("%d",&n);
		if(n&1);
		else count++; 		
	}
	printf("%d valores pares\n",count );
	return 0;
}

