#include <bits/stdc++.h>

using namespace std;

int main() 
{	
	string s;
	int i;
	scanf("%s",s);
	if(s[0]=='-') printf("-\n");
	for(i=0; s[i]; i++)
	{
		if(s[i]>'0' && s[i]<='9') break;
	}
	for(int j=i; s[j]; j++)
	return 0;
}